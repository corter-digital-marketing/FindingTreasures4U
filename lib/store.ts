// Where customers collect in-store pickup orders.
export const STORE_ADDRESS = "347 East Main Street, Lock Haven, PA";

// Public contact email shown on the site.
export const STORE_EMAIL = "jamesp8241@comcast.net";

// Public phone number shown in the About section. Set to null until we have
// one — the About section hides the line entirely rather than show a blank
// or fake number.
export const STORE_PHONE: string | null = null;
